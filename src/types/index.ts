export interface Product {
  id: string;
  index: string;
  name: string;
  tagline: string;
  problem: string;
  decision: string;
  result: string;
  image: string;
  demoUrl?: string;
  githubUrl?: string;
}